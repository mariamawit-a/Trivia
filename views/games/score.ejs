<%
      function shuffle(array) {
        array.sort(() => Math.random() - 0.5);
        }
      let choices = [m.correct_answer];
      choices = choices.concat(m.incorrect_answers);
      shuffle({choices});
%>

